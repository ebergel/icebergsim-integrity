"""icebergsim_integrity — ISM_LD_beta, the trial integrity instrument.

Governed by CONSTITUTION-LD.md. The package takes its own name, never the
ancestor's (`icebergsim` is ISM_RCT_R, consumed as a dependency — L9).
"""

__version__ = "0.1.0"
